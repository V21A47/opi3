<project name="${projectName}" default="build">

    <property file="ant.properties"/>

    <target name="version">
        <echo message="Using Java version ${ant.java.version}."/>
        <echo>Java/JVM version: ${ant.java.version}</echo>
        <echo>Java/JVM detail version: ${java.version}</echo>
    </target>

    <target name="compile">
        <mkdir dir="${build}"/>
        <javac destdir= "${build}" includeAntRuntime="no" includeantruntime="true" deprecation="true" target="1.7" source="1.7">
                <src path="${src}" />
        </javac>
    </target>

    <target name="build" depends="compile">
        <manifest file="${build}/MANIFEST.MF">
            <attribute name="Main-Class" value="${mainClass}"/>
            <attribute name="Specification-Version" value="${specificationVersion}"/>
            <attribute name="Implementation-Version" value="${implementationVersion}"/>
        </manifest>

        <jar destfile="${projectName}.jar" manifest="${build}/MANIFEST.MF">
            <fileset dir="${build}">
                <include name="*.class"/>
            </fileset>
        </jar>
    </target>

    <target name="clean">
        <delete dir="${build}"/>
        <delete file="${projectName}.jar"/>
    </target>

    <target name="test" depends="build">

    </target>
</project>
